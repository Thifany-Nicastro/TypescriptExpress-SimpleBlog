import CreatePostService from '../services/CreatePostService';

describe('CreatePost', () => {
  it('should be able to create a new post', () => {
    expect(1+2).toBe(3);
  });

  // it('should not be able to create two posts with the same title', () => {
  //   expect(1+2).toBe(3);
  // });
});